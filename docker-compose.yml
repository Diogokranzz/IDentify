services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: identify-postgres
    environment:
      POSTGRES_USER: identify
      POSTGRES_PASSWORD: identify
      POSTGRES_DB: identify
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
